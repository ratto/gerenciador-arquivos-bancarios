# DEV REPORT — [Nome da Funcionalidade] — Ciclo [N]

> Escrito pelo AGENT_DEV após concluir todas as tarefas.
> Lido pelo AGENT_QA.

## Resumo

Breve descrição do que foi implementado.

## Status

- [ ] Todas as tarefas do TASKS.md concluídas
- [ ] `npm run lint` passou (cole a última linha da saída)
- [ ] `npm test` passou (cole a linha de resumo, ex.: "✓ 42 tests passed")

## Arquivos Alterados

Liste todos os arquivos criados ou modificados:

| Arquivo | Tipo de mudança | Descrição |
|---------|----------------|-----------|
| `src/model/lib/cnab/cnab240.ts` | criado | Construtor de linha CNAB240 (Model) |
| `src/stores/cnab-store.ts` | criado | Pinia store para leiaute ativo |
| `src/components/CnabForm.vue` | criado | Componente de formulário dinâmico |
| `src/stores/__tests__/cnab-store.test.ts` | criado | Testes unitários do store |

## Decisões de Arquitetura

Documente decisões que desviem ou estendam a spec original:

1. Decisão: ...
   Motivo: ...

## Limitações Conhecidas / Dívida Técnica

- ...

## Saída dos Testes

Cole a saída completa de `npm test` aqui:

```
(cole a saída)
```

## Saída do Lint

Cole a saída completa de `npm run lint` aqui:

```
(cole a saída)
```

## Notas para o QA

Algo específico que o AGENT_QA deve observar:

- ...
